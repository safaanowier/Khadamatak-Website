@model IEnumerable<_mvcproject_.Models.Order>
@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layoutprovider.cshtml";
    //ApplicationUser provider = Model?.FirstOrDefault().Provider;

}

<!--------------------------------------------------------------------->
<style>
    .checked {
        color: orange;
    }
</style>
<div class="row py-5 px-4">
    <div class="col-md-10 mx-auto">
        <!-- Profile widget -->
        <div class="bg-white shadow rounded overflow-hidden">
            @*<div class="px-4 pt-0 pb-4 cover">
                <div class="media align-items-end profile-head">

                    <div class="profile mr-3">
                        <img src="~/Home//images/default.png" alt="profile photo" width="130" class="rounded mb-2 img-thumbnail">
                        <a asp-action="Edit" asp-route-id="@provider.Id" class="btn btn-outline-dark btn-sm btn-block">Edit profile</a>
                    </div>

                    <div class="media-body mb-5 text-white">
                        <h4 class="mt-0 mb-0">@provider.FirstName @provider.LastName</h4>
                        <p class="small mb-4"> <i class="fas fa-map-marker-alt mr-2"></i>Provider</p>
                    </div>
                </div>
            </div>*@
            @*<div class="bg-light p-4 d-flex justify-content-end text-center">
                    <div>
                      <a asp-controller="Order" asp-action="GetRejectOrderByProviderId" asp-route-id="@provider?.Id"  class="btn btn-outline-dark" data-mdb-ripple-color="dark" style="z-index: 1;">
                Rejected
              </a>
              </div>
              <div class="px-3">
                     <a asp-controller="Order" asp-action="GetNewOrdersByProviderId" asp-route-id="@provider?.Id"  class="btn btn-primary position-relative"  >
                 New  @*<span class="bi bi-bell-fill" style="color: blue"></span>*@</a>
  
         @*     </div>
              <div>
                <a asp-controller="Order" asp-action="GetAcceptOrdersByProviderId" asp-route-id="@provider?.Id"  class="btn btn-outline-dark" data-mdb-ripple-color="dark" style="z-index: 1;">
                History
              </a>
              </div>
            </div>*@
         

             

                    <div class="py-4 px-4">
                <div class="d-flex align-items-center justify-content-between mb-3">
                <h5 class="mb-0">Your Orders</h5>
                </div>

                <table class="table table-bordered table-light">
                <thead>
                <tr>
                <th scope="col"> User</th>
                <th scope="col">Message</th>
                <th scope="col">Price</th>
                <th scope="col">PhoneNumber</th>
                
                <th scope="col">Rating</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model)
                {
                <tr>

                <th scope="row">@item.User.FirstName @item.User.LastName</th>
                <td>@item.Message</td>
                <td>@item.price</td>
                <td>@item.User.PhoneNumber</td>
            <td>
                                    <span class="hint-star star">
                                        @for (int i = 0; i < item.Rating; i++)
                                        {
                                            <span class="fa fa-star checked"></span>
                                        }

                                        @for (int i = 0; i < 5 - item.Rating; i++)
                                        {
                                            <span class="fa  fa-star-o"></span>
                                        }
                                    </span>
                                </td>
                </tr>
                }

                </tbody>
                </table>


                </div>
        </div>
    </div>
</div>

